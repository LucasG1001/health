# Conexão local de desenvolvimento (crie o banco "health" no seu Postgres).
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/health

# Porta da API (o frontend em dev faz proxy de /api para esta porta).
PORT=3333

# Diretório onde as imagens enviadas são gravadas (default: ./uploads).
UPLOAD_DIR=
